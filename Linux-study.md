*find / -type f -name lemlem.pdf*  those are a command to find a file name called lemlem.pdf 
or you can used 
*grep* search  for a particular keyword
 *piping* is allow us to take the output of one command and send it as input to another command *|* 
*ps* use the ps (process) command to know all process running on my linux sys

- modifying files and directories

*create files*
- concatination with *cat*
cat use for display the content of the file but using *>* redirect use for want to crate like 
*cat > lemlem.pdf* and to quit *ctrl-D*

*sed* : to find and replace
sed command lets you search for occurrences of a word or a text pattern and then perform some action on it 

team , individul , elegance 
write content with multiple profile 
digital signture