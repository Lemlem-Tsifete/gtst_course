 ## Outputs and comments

`print()` : for display the output 
`sep = ", "` : when display separate by comma or put on the " " in side this one print function 
eg. `print("Hello world!", "Hi my name is lemlem", sep=", ")` we can use like this 

`end=" "`
eg `print("Hello world", "Hi my name is lemlem",end = " " 
## comment 
this sign is for single line comment "#" 
this sign for multiline comment "  ' ' '    ' ' '  "
## python keywords
## Variables 

- the process of giving value to word is called variable declaration
- the word that holds that data is called identifier
### f print
`num = 30
`print(f"The 2 number is {num}"`
### Data type
1. numeric datatype 
2. string data type you can use `type()` function to know the data type  
3. sequence data type : hold number of item 
		a. Lists : collection of similar of different types of items List using square bracket Using `" items "`
		you can modify  the items 
		b. Tupls :  order sequence items  and it immutable  use circle bracket 
		c. dictionary : using key and value pair 

[] - square bracket , () - circle bracket, {} - curly bracket

